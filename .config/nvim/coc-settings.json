{
	"languageserver": {
		"julia": {
			"command": "/usr/local/bin/julia",
			"args": ["--startup-file=no", "--history-file=no", "-e", "using LanguageServer, Pkg; import StaticLint, SymbolServer; env_path = dirname(Pkg.Types.Context().env.project_file); debug = false; server = LanguageServer.LanguageServerInstance(stdin, stdout, debug, env_path); server.runlinter = true; run(server);"],
			"filetypes": ["julia"]
		}
	}
}
